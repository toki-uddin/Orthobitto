# Orthobitto v4.0.8 — Result Flow Fix

- Desktop translation rendering explicitly reveals the result card and hides the empty state.
- Mobile bottom navigation now exposes Sentences and Words directly.
- Approved vocabulary is built in the background immediately after OCR.
- Translation completion automatically opens Sentence results on mobile.
- Mobile result badges expose sentence/word counts.
- Cache-bust/service-worker version bumped to 4.0.8.
