# Orthobitto v4.1.2 - root files

This release is intentionally flat for GitHub/Vercel deployment. Extract the release ZIP and place these files directly in the repository root.

Core: `index.html`, `app.css`, `app.js`, `orthobitto-enhancements.js`.
Engines: `dictionary-engine.js`, `content-safety.js`, `word-classes.js`, `pdf-generator.js`.
Data: `orthobitto-dictionary.json`, `dictionary-inline.js`, `orthobitto-relations.json`, `orthobitto-lexicon-10k.txt`.
PWA: `manifest.json`, `sw.js`, icons, `vercel.json`.
GitHub: `README.md`, `.gitignore`, `.gitattributes`, `GITHUB-UPLOAD.md`.
QA/tooling: `build_dictionary.py`, `validate_release.py`.
